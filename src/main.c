#include "hdr/menuPrincipal.h"
#include "hdr/auxiliares.h"

int main(){
	verificaPasta(arquivos);
	verificaArquivo(especie);
	verificaArquivo(especieIndice);
	verificaArquivo(individuo);
	verificaArquivo(individuoIndice);
	verificaArquivo(captura);
	verificaArquivo(capturaIndice);
	mostraMenuPrincipal();
	return 0;
}
